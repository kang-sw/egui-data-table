{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build.Demo",
            "type": "shell",
            "group": "build",
            "command": "cargo build --example demo",
            "problemMatcher": "$rustc"
        }
    ]
}